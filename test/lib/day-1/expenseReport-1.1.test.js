import assert from 'assert';
import { solve } from '../../../lib/day-1/expenseReport-1.1.js';

describe('Expense Report 1.1 - Solve', () => {
    it('should return 514579', () => {
        const relativeFilePath = 'srv/day-1/expense-report.txt';
        // assert.strictEqual(solve(relativeFilePath), 514579);
    });
});